# Core data stack
pandas>=2.1,<3
numpy>=1.24,<3

# Plotting / pitch
matplotlib>=3.8,<4
mplsoccer>=1.2,<2

# Graphs for passing network
networkx>=3.1,<4

# Modeling
xgboost>=1.7.6,<2.0        # avoids older fit/eval_metric quirks you saw
scikit-learn>=1.4,<2.0     # ensures CalibratedClassifierCV(estimator=...) & OHE behavior
joblib>=1.3,<2

# Data access
requests>=2.31,<3
statsbombpy>=1.7,<2        # optional; we gracefully fall back to raw GitHub if missing

# App
streamlit>=1.30,<2         # guarantees st.rerun() is available
